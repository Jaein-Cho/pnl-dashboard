# 프로젝트 규칙

이 문서는 이 프로젝트에서 매번 별도로 말하지 않아도 항상 지켜야 하는 규칙입니다.

## 1. 파일 경로 하드코딩 금지
- 코드에 특정 파일 경로를 직접 써넣지 않는다.
- 데이터 파일은 사용자가 그때그때 업로드/지정한 파일을 사용하도록 코드를 작성한다 (예: 파일 업로드 컴포넌트, 인자/환경변수로 경로 전달 등).

## 2. 데이터 파일(엑셀·CSV) 관리
- 엑셀(.xlsx, .xls)·CSV 파일은 앱 소스 코드 폴더 밖에 둔다.
- `.gitignore`에 등록하여 버전관리 및 배포에 포함되지 않도록 한다.

## 3. API 키 관리
- API 키, 비밀번호 등 민감 정보는 코드에 직접 넣지 않는다.
- `.env` 파일에서 읽어오도록 구현하고, `.env`는 `.gitignore`에 포함한다.

## 4. 응답 언어
- 모든 답변은 항상 한국어로 한다.
